# This file is part of RAVL, Recognition And Vision Library
# Copyright (C) 2001, University of Surrey.
# This code may be redistributed under the terms of the GNU lesser
# public license (LGPL). See the lgpl.licence file for details or
# see http://www.gnu.org/copyleft/lesser.html
# file-header-ends-here
ifndef GTKDEF
GTKDEF = 1
################################################
# $Id$

include x11.def

ifeq (x,x)

#ifeq ($(ARC),linux)
#GLIBPATH = /usr
#GTKPPATH = /usr
#else
#GLIBPATH = /opt/PDgtk/glib-1.2.7
#GTKPPATH = /opt/PDgtk/gtk+-1.2.7
#endif

#GTKCONFIG = $(GTKPPATH)/bin/gtk-config

GTKCONFIG = gtk-config
GLIBCONFIG = glib-config 

GTKCFLAGS = `$(GTKCONFIG) --cflags`
GTKCFLAGS += `$(GLIBCONFIG) --cflags`

GTKLIBS =  -L/opt/lib `$(GTKCONFIG) --libs`
GTKLIBS += `$(GLIBCONFIG) --libs`

GLIBCFLAGS = `$(GLIBCONFIG) --cflags`
GLIBCFLAGS = `$(GLIBCONFIG) --cflags`



#ifeq ($(ARC),sol2)
#GTKLIBS += -R$(GTKPPATH)/lib -R$(GLIBPATH)/lib
#endif

#ifeq ($(ARC),sgi)
#GTKLIBS += -Wl,-rpath,$(GTKPPATH)/lib,-rpath,$(GLIBPATH)/lib
#endif

else
GTKCFLAGS= `pkg-config gtk+-2.0 --cflags` -DRAVL_USE_GTK2=1
GTKLIBS= `pkg-config gtk+-2.0 --libs`
endif

INCLUDES += $(GTKCFLAGS)
EXELIB := $(GTKLIBS) $(EXELIB)

endif
