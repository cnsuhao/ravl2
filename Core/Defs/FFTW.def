ifndef FFTW_DEF
FFTW_DEF=1


ifneq ($(filter $(ARC), sol2_7 sol2_9),)
FFTWHOME=/vol/vssp/dera/fft
INCLUDES := -I$(FFTWHOME)/include $(INCLUDES)
EXELIB := -L$(FFTWHOME)/lib -lfftw $(EXELIB)
endif



ifeq ($(ARC), linux)
FFTWHOME=/vol/vssp/local/tools
INCLUDES := -I$(FFTWHOME)/include $(INCLUDES)
EXELIB := -L$(FFTWHOME)/lib -lfftw $(EXELIB)
endif

endif
