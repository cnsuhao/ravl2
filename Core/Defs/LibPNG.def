# This file is part of RAVL, Recognition And Vision Library
# Copyright (C) 2001, University of Surrey.
# This code may be redistributed under the terms of the GNU lesser
# public license (LGPL). See the lgpl.licence file for details or
# see http://www.gnu.org/copyleft/lesser.html
# file-header-ends-here
ifndef LIBPNG_DEF
LIBPNG_DEF = 1

include Math.def
include zlib.def





EXELIB := -lpng $(EXELIB)

#LIBPNG_HOME=/home
#INCLUDES += -I$(LIBPNG_HOME)/include
#EXELIB := -L$(LIBPNG_HOME)/lib $(EXELIB)


ifeq ($(ARC),sgi)
ifndef LIBPNG_HOME
LIBPNG_HOME=/opt/PDlibs/
endif 
INCLUDES += -I$(LIBPNG_HOME)/include 
EXELIB := -Wl,-rpath,$(LIBPNG_HOME)/lib -L$(LIBPNG_HOME)/lib  $(EXELIB)
else

#ifeq ($(ARC),sol2)
ifneq ($(filter $(ARC), sol2_7 sol2_9),)
ifndef LIBPNG_HOME
LIBPNG_HOME=/opt/PDlibs/png-1.0.7
endif
INCLUDES += -I$(LIBPNG_HOME)/include 
EXELIB := -L$(LIBPNG_HOME)/lib $(EXELIB)


endif

endif

endif
