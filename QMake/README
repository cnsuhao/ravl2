
QMake
-----

To install QMake use the install script.

./install [location].

Use the following alias for compilation.  PROJECT_OUT controls the directory 
into which your own code will be compiled. The default is 'build' in your home directory. 
alias mk '${MAKE} -f [location]/share/RAVL/QMake/QMake.mk PROJECT_OUT=$HOME/build'

